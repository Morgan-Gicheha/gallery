pipeline {
    agent any

    stages {
        stage('requirements') {
            steps {
                echo 'npm install'
            }
        }
        stage('Run APP') {
            steps {
                echo 'npm start'
            }
        }
    }
}